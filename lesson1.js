// Data types.
// String
// Number
// Bigint
// Boolean
// Undefined
// Null
// Symbol
// Object
// Array

// A string is a series of characters like "John Doe".
// Strings are written with quotes.
const car = "Ford";

// Numbers
const x = 12;

// BigInt
// All JavaScript numbers are stored in a a 64-bit floating-point format.

let y = BigInt("123456789012345678901234567890");

// Booleans can only have two values: true or false

const isTrue = true;
const isFalse = false;

// JavaScript arrays are written with square brackets.
// Array items are separated by commas.

const cars = ["Saab", "Volvo", "BMW"];

// JavaScript objects are written with curly braces {}.
// Object properties are written as name:value pairs, separated by commas.

const person = {firstName:"John", lastName:"Doe", age:50, eyeColor:"blue"};
// person.firstName would equal "John"


// In JavaScript, a variable without a value, has the value undefined. The type is also undefined.

let bag;    // Value is undefined, type is undefined

// A variable without a value intentionally is null

const stuff = null;

// Operators
// +	Addition
// -	Subtraction
// *	Multiplication
// **	Exponentiation (ES2016)
// /	Division
// ++	Increment
// --	Decrement


// Operator	Example	       Same As
// =	    x = y	       x = y
// +=	    x += y	       x = x + y
// -=	    x -= y	       x = x - y
// *=	    x *= y	       x = x * y
// /=	    x /= y	       x = x / y
// **=	    x **= y	       x = x ** y

// ==	equal to
// ===	equal value and equal type
// !=	not equal
// !==	not equal value or not equal type
// >	greater than
// <	less than
// >=	greater than or equal to
// <=	less than or equal to
// ?	ternary operator

// &&	logical and
// ||	logical or
// !	logical not




// What are variables? Variables are containers for storing data
//If you want a general rule: always declare variables with const.
//If you think the value of the variable can change, use let

// Examples
const num1 = 5; // This value can't be changed
let num2 = 7; // This value can be changed

// Write two const and two let variables that have the value of a string

// Write two const and two let variables that have the value of a number

// Write a const for each of the boolean types

// Write a const that is an object

// Write a const that is an array


// Examples of variable being assigned and that value being changed
let test1 = 9;
test1 = test1 + 5;

let test2 = "Hello";
test2 = test2 + " World";

// Practice

// Create a new variable and have it's value be the comparison of two of your variables

console.log('yourVarHere')

// Create a new variable by combinding two variables that have string data types

console.log('yourVarHere')

// Show how you would the value at the first index of your array

console.log('arrayValueHere')

// Show me how you would get a value from your object

console.log('answerHere')

// Compare any two variables and see if they are equal

console.log('answerHere')

// Check if one of your number variables is greater than the other

console.log('answerHere')

// Console log a boolean value

console.log('answerHere')

// Create a new variable and use the += operator

console.log('answerHere')
